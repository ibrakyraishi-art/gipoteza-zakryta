# 🚀 Деплой Next.js на Timeweb VPS

## 📋 Что вам понадобится

- ✅ VPS на Timeweb (минимум 1GB RAM, Ubuntu 20.04/22.04)
- ✅ Домен (опционально, можно использовать IP)
- ✅ SSH клиент (PuTTY для Windows или встроенный терминал)
- ✅ 1-2 часа времени

## 💰 Тарифы Timeweb VPS

Рекомендую минимум:
- **VPS-1**: 1 CPU, 1GB RAM, 10GB SSD (~290₽/мес)
- **VPS-2**: 1 CPU, 2GB RAM, 20GB SSD (~490₽/мес) - лучше!

## 🎯 Пошаговая инструкция

### Шаг 1: Заказ и настройка VPS

1. **Заказ VPS на Timeweb**:
   - Зайдите на https://timeweb.com/ru/services/vps
   - Выберите тариф (минимум VPS-1)
   - ОС: **Ubuntu 22.04 LTS**
   - Оформите заказ

2. **Получите данные для входа**:
   - IP адрес
   - Root пароль
   - (придут на email)

### Шаг 2: Подключение к VPS

#### Для Windows:

**Вариант A: PuTTY**
1. Скачайте PuTTY: https://www.putty.org/
2. Откройте PuTTY
3. Host Name: `ваш-IP-адрес`
4. Port: `22`
5. Connection type: `SSH`
6. Нажмите "Open"
7. Login: `root`
8. Password: (пароль из email)

**Вариант B: PowerShell (Windows 10/11)**
```powershell
ssh root@ваш-IP-адрес
# Введите пароль
```

#### Для Linux/Mac:

```bash
ssh root@ваш-IP-адрес
# Введите пароль
```

### Шаг 3: Первичная настройка сервера

```bash
# Обновление системы
apt update && apt upgrade -y

# Установка необходимых пакетов
apt install -y curl wget git nano ufw

# Настройка firewall
ufw allow 22      # SSH
ufw allow 80      # HTTP
ufw allow 443     # HTTPS
ufw enable
```

### Шаг 4: Установка Node.js

```bash
# Установка Node.js 18.x (LTS)
curl -fsSL https://deb.nodesource.com/setup_18.x | bash -
apt install -y nodejs

# Проверка установки
node --version   # должно показать v18.x.x
npm --version    # должно показать 9.x.x

# Установка глобальных пакетов
npm install -g pm2
```

### Шаг 5: Создание пользователя (безопасность)

```bash
# Создаем пользователя для приложения
adduser nodejs
# Введите пароль и подтвердите (остальное можно пропустить Enter)

# Добавляем в группу sudo (опционально)
usermod -aG sudo nodejs

# Переключаемся на нового пользователя
su - nodejs
```

### Шаг 6: Загрузка проекта

**Вариант A: Через Git (рекомендуется)**

```bash
# Если проект на GitHub
cd ~
git clone https://github.com/ваш-username/gipoteza-zakryta.git
cd gipoteza-zakryta

# Установка зависимостей
npm install

# Сборка проекта
npm run build
```

**Вариант B: Через FTP/SFTP**

Можно использовать FileZilla или WinSCP:
1. Подключитесь по SFTP к вашему серверу
2. Загрузите всю папку проекта в `/home/nodejs/gipoteza-zakryta`
3. Затем через SSH:
```bash
cd ~/gipoteza-zakryta
npm install
npm run build
```

### Шаг 7: Настройка PM2

```bash
# Запуск приложения через PM2
pm2 start npm --name "gipoteza-zakryta" -- start

# Проверка статуса
pm2 status

# Логи (если нужно)
pm2 logs gipoteza-zakryta

# Автозапуск при перезагрузке сервера
pm2 startup
pm2 save
```

Приложение запустилось на порту 3000! Но пока доступно только локально.

### Шаг 8: Установка и настройка Nginx

```bash
# Выходим обратно в root
exit

# Установка Nginx
apt install -y nginx

# Создаем конфигурацию для сайта
nano /etc/nginx/sites-available/gipoteza-zakryta
```

**Скопируйте этот конфиг:**

```nginx
server {
    listen 80;
    server_name ваш-домен.ru www.ваш-домен.ru;
    # Или если домена нет, укажите IP:
    # server_name ваш-IP-адрес;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

**Сохраните:** `Ctrl+X`, затем `Y`, затем `Enter`

```bash
# Создаем символическую ссылку
ln -s /etc/nginx/sites-available/gipoteza-zakryta /etc/nginx/sites-enabled/

# Удаляем дефолтный конфиг
rm /etc/nginx/sites-enabled/default

# Проверяем конфигурацию
nginx -t

# Перезапускаем Nginx
systemctl restart nginx
systemctl enable nginx
```

### Шаг 9: Настройка домена (если есть)

В панели вашего регистратора доменов (REG.RU, Timeweb и т.д.):

**DNS записи:**
```
Тип: A
Имя: @
Значение: ваш-IP-адрес

Тип: A
Имя: www
Значение: ваш-IP-адрес
```

⏱️ Подождите 5-30 минут для распространения DNS.

### Шаг 10: Установка SSL (Let's Encrypt)

```bash
# Установка Certbot
apt install -y certbot python3-certbot-nginx

# Получение SSL сертификата
certbot --nginx -d ваш-домен.ru -d www.ваш-домен.ru

# Следуйте инструкциям:
# - Введите email
# - Согласитесь с условиями (Y)
# - Выберите опцию 2 (redirect)

# Автопродление (проверка)
certbot renew --dry-run
```

**Готово!** Ваш сайт доступен по HTTPS! 🎉

### Шаг 11: Проверка

Откройте в браузере:
- `http://ваш-домен.ru` (должен редиректить на HTTPS)
- `https://ваш-домен.ru` (ваш сайт!)

## 🔄 Обновление сайта

### Через Git:

```bash
# Подключитесь к VPS
ssh nodejs@ваш-IP-адрес

# Перейдите в папку проекта
cd ~/gipoteza-zakryta

# Получите изменения
git pull

# Установите новые зависимости (если есть)
npm install

# Пересоберите проект
npm run build

# Перезапустите PM2
pm2 restart gipoteza-zakryta
```

### Через SFTP:

1. Подключитесь по SFTP
2. Загрузите измененные файлы
3. Через SSH:
```bash
cd ~/gipoteza-zakryta
npm install
npm run build
pm2 restart gipoteza-zakryta
```

## 🔧 Полезные команды

### PM2:

```bash
pm2 status                      # Статус приложений
pm2 logs gipoteza-zakryta       # Просмотр логов
pm2 restart gipoteza-zakryta    # Перезапуск
pm2 stop gipoteza-zakryta       # Остановка
pm2 delete gipoteza-zakryta     # Удаление из PM2
pm2 monit                       # Мониторинг в реальном времени
```

### Nginx:

```bash
systemctl status nginx          # Статус
systemctl restart nginx         # Перезапуск
systemctl reload nginx          # Перезагрузка конфига
nginx -t                        # Проверка конфигурации
tail -f /var/log/nginx/error.log  # Логи ошибок
```

### Системные:

```bash
htop                           # Мониторинг ресурсов (apt install htop)
df -h                          # Свободное место
free -m                        # Использование RAM
reboot                         # Перезагрузка сервера
```

## 🛡️ Безопасность

### 1. Смена SSH порта (опционально):

```bash
nano /etc/ssh/sshd_config
# Найдите строку Port 22
# Измените на Port 2222 (или другой)
# Сохраните

ufw allow 2222
systemctl restart sshd

# Теперь подключаться нужно так:
# ssh -p 2222 nodejs@ваш-IP
```

### 2. Отключение входа root:

```bash
nano /etc/ssh/sshd_config
# Найдите: PermitRootLogin yes
# Измените на: PermitRootLogin no
# Сохраните

systemctl restart sshd
```

### 3. Установка Fail2Ban:

```bash
apt install -y fail2ban
systemctl enable fail2ban
systemctl start fail2ban
```

## 📊 Мониторинг

### Установка мониторинга (опционально):

```bash
# PM2 Plus (бесплатный базовый мониторинг)
pm2 link [secret-key] [public-key]
# Ключи можно получить на https://app.pm2.io
```

## ❓ Решение проблем

### Сайт не открывается:

```bash
# Проверьте статус приложения
pm2 status

# Проверьте логи
pm2 logs gipoteza-zakryta

# Проверьте Nginx
systemctl status nginx
nginx -t

# Проверьте firewall
ufw status
```

### Ошибка при сборке (npm run build):

```bash
# Проверьте память
free -m

# Если мало памяти, создайте swap:
fallocate -l 2G /swapfile
chmod 600 /swapfile
mkswap /swapfile
swapon /swapfile
echo '/swapfile none swap sw 0 0' >> /etc/fstab
```

### Приложение падает:

```bash
# Посмотрите логи
pm2 logs gipoteza-zakryta --lines 100

# Увеличьте память для Node.js
pm2 delete gipoteza-zakryta
pm2 start npm --name "gipoteza-zakryta" --node-args="--max-old-space-size=512" -- start
```

## 💰 Оптимизация расходов

Для экономии:
- Используйте VPS-1 (достаточно для начала)
- Настройте swap для экономии RAM
- Оптимизируйте изображения на сайте
- Используйте Cloudflare (бесплатный CDN)

## 📈 Масштабирование

Когда трафик вырастет:
1. Увеличьте тариф VPS
2. Настройте кэширование в Nginx
3. Используйте CDN (Cloudflare)
4. Настройте PM2 cluster mode:
```bash
pm2 start npm --name "gipoteza-zakryta" -i max -- start
```

## 🆘 Поддержка

**Если что-то не работает:**

1. Проверьте логи: `pm2 logs`
2. Проверьте Nginx: `nginx -t`
3. Проверьте firewall: `ufw status`
4. Напишите в Telegram: https://t.me/gipoteza_zakryta

## ✅ Чеклист финальной проверки

- [ ] VPS создан и доступен
- [ ] Node.js установлен
- [ ] Проект загружен и собран
- [ ] PM2 запущен и работает
- [ ] Nginx настроен
- [ ] Firewall настроен
- [ ] Домен привязан (если есть)
- [ ] SSL установлен
- [ ] Сайт открывается по HTTPS
- [ ] PM2 автозапуск настроен

---

**Готово! Ваш сайт работает на Timeweb VPS! 🎉**

Время настройки: 1-2 часа
Стоимость: от 290₽/мес

**P.S.:** Хотя Vercel проще и бесплатен 😉
