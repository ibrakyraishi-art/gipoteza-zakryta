---
title: "Что такое AppsFlyer ID и зачем он нужен"
description: "AppsFlyer ID — это внутренний уникальный идентификатор, который создаётся при первом запуске приложения. Он формируется всегда — вне зависимости от ATT."
date: "2025-09-28"
tags: ["MMP", "AppsFlyer", "Атрибуция", "iOS"]
status: "published"
---

**AppsFlyer ID (или appsflyer_id)** — это внутренний уникальный идентификатор, который создаётся SDK AppsFlyer **при первом запуске приложения**. Он формируется всегда — вне зависимости от того, дал пользователь согласие на ATT или нет.

📌 Это основной идентификатор, на который опирается весь мобильный трекер: он связывает события установки, сессии, покупки между собой.

## Что делает AppsFlyer ID

Если IDFA есть — он тоже участвует в связке, но **даже при наличии IDFA вся внутренняя аналитика строится от appsflyer_id**.

Если IDFA нет (например, пользователь не дал согласия на ATT), то **appsflyer_id становится единственной опорной точкой** для связки всех действий.

→ То есть: события внутри трекера (вплоть до покупок) будут принадлежать этому appsflyer_id, даже если другие ID недоступны.

## Проблема: как работает reinstall без ATT

Если пользователь **установил приложение и не дал согласие на ATT**, то при **удалении и последующей переустановке**:

- создаётся **новый appsflyer_id**
- и весь цикл считается заново — как будто это абсолютно новый install

### Почему?

Потому что при отсутствии IDFA, трекер **не может связать reinstall с предыдущей установкой**, и в отчётах это будет отображено как **новый install**, а все действия пользователя (включая покупку) будут восприниматься как от «нового юзера».

## Что это значит на практике

AppsFlyer ID живёт только в рамках одной установки. После удаления — всё обнуляется (если нет IDFA или IDFA установки не совпадает с IDFA при переустановке).

⚠️ В отчётах вы увидите:

- рост install
- рост уникальных покупок
- рост "новичков"

Хотя по факту это может быть **тот же пользователь, просто переустановивший приложение**.

## Почему это важно

Все **уникальные события** в AppsFlyer (включая первую покупку) считаются **по appsflyer_id**, а не по пользователю.

Если пользователь удалил приложение, а потом вернулся — для трекера это может быть **полностью новый клиент**. (если нет IDFA)

📉 Это ведёт к:

- завышенным метрикам по install
- искаженному количеству новичков
- **искусственно заниженному CAC**

## Как считать честно: Customer User ID

Чтобы корректно считать реальных новичков, одного AppsFlyer ID недостаточно.

Нужна точка, которая **переживёт reinstall и не зависит от ATT** — это **ваш собственный идентификатор**, например:

- e-mail
- номер телефона

## Как работает связка с Customer User ID

1. Пользователь устанавливает приложение и оформляет подписку → вы сохраняете его e-mail или номер
2. Через 2 недели он удаляет приложение и снова устанавливает (без ATT)
3. AppsFlyer создаёт **новый appsflyer_id**
4. Но при логине через e-mail вы узнаёте, что это **тот же пользователь**
5. Повторная покупка не считается уникальной — вы видите, что это не новичок, а возврат

## Что это даёт

С помощью Customer User ID:

- Вы **реально знаете**, кто новый пользователь, а кто вернулся
- Не считаете повторные покупки за новые
- **Честно считаете CAC**, не завышая эффективность канала

## Итог

AppsFlyer ID — это фундамент для работы трекера, он нужен для базовой атрибуции внутри приложения.

Но если хотите честно считать новичков, CAC — без **Customer User ID** это не точно.

Потому что одна установка ≠ один пользователь, особенно в iOS с отключённым ATT.
