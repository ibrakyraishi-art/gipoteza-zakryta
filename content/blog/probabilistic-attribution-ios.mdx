---
title: "Вероятностная атрибуция в iOS: как она устроена сейчас"
description: "После выхода iOS 14.5 классическая детерминированная атрибуция используется всё реже. В большинстве случаев iOS-трафик уходит в вероятностную модель."
date: "2025-09-22"
tags: ["iOS", "Атрибуция", "SKAN", "MMP"]
status: "published"
---

После выхода iOS 14.5 классическая детерминированная атрибуция используется всё реже.

Чтобы сработала **детерминированная атрибуция**, пользователь должен дать согласие через ATT **и в вашем приложении, и у паблишера**, где он увидел рекламу.

Если хотя бы на одной стороне нет ATT-согласия — **детерминированная атрибуция не сработает**.

В большинстве случаев iOS-трафик уходит в **вероятностную модель**.

⚠️ Частота согласия пользователя на ATT зависит от ниши, узнаваемости приложения, источника и других факторов.

## Как это работало раньше

1. Источник показал рекламу
2. Пользователь кликнул и установил приложение
3. IDFA был доступен и у источника, и у трекера
4. Атрибуция проходила детерминированно

## Как это работает сейчас

Если хотя бы одна сторона не получила ATT-согласие — детерминированная атрибуция невозможна, и установка атрибутируется через вероятностную модель или не атрибутируется.

## Какие вероятностные атрибуции используются сейчас

### 1. SKAdNetwork (SKAN) — фреймворк от Apple

Работает на агрегированных данных с ограничениями:

- Данные приходят с задержкой
- Нет аналитики на уровне креативов
- **Пороги видимости (thresholds)** — если в кампании недостаточно конверсий, события просто не отображаются

SKAN даёт общее понимание по каналам, но не подходит для детальной оптимизации.

### 2. Вероятностная атрибуция от MMP (AppsFlyer, Adjust и др.)

Используется с non-SRN источниками:

- Mintegral
- Unity
- Яндекс
- Hybe и др.

Что делает трекер:

- Получает событие установки
- Сравнивает IP, user agent, тайминги
- Назначает источник, исходя из модели вероятности

Каждый MMP использует собственный алгоритм. Это объясняет расхождения между отчётами разных платформ.

### 3. SRN-источники — со своими фреймворками

- TikTok → **ADC (Advanced Data Collection)**
- Facebook → **AEM (Aggregated Event Measurement)**
- Google — использует probabilistic-атрибуцию в ограниченном виде:
  - нужно подключать **Firebase**
  - активируется только через поддержку Google

💡 Один из обходных путей: запускать **web-to-app кампании** через **веб** кампанию и использовать **трекинговую ссылку от MMP**.

В этом случае атрибуцию проводит сам мобильный трекер (от начала до конца), а не SRN, — и данные получаются стабильнее. Иначе, если запустить Google UAC на iOS без вероятностной атрибуции, показатели будут крайне слабыми (можно смотреть по SKAN).

## Что важно учитывать

Если вы закупаете трафик через DSP или SSP (Mintegral, Unity, Hybe, AppLovin, Moloco) — с высокой долей вероятности вы работаете через **probabilistic-атрибуцию от трекера**.

SKAN работает криво. Deterministic требует согласия на ATT и у вас, и у паблишера. В остальных случаях — атрибуция строится через вероятностные модели.

## Что дальше

Атрибуция в iOS работает иначе — и это важно учитывать при анализе.

Это не значит, что данные неверны. Но чтобы правильно интерпретировать эффективность, важно понимать:
- по какой схеме происходила атрибуция,
- использовалась ли модель SKAN, алгоритмы трекера (MMP) или фреймворки SRN-источников.

В следующих постах разберу:
- где чаще всего возникают искажения,
- как не перепутать рост с особенностями трекинга,
- и на что обращать внимание, если цифры кажутся слишком хорошими.
