# Инструкция по запуску проекта "Гипотеза закрыта"

## 📋 Предварительные требования

Для запуска проекта вам потребуется:

1. **Node.js** версии 18.x или выше
2. **npm**, **yarn** или **pnpm** (менеджер пакетов)

### Установка Node.js

Если Node.js не установлен:

#### Windows
Скачайте установщик с официального сайта: https://nodejs.org/
Рекомендуется версия LTS (Long Term Support)

#### Проверка установки
```bash
node --version
npm --version
```

## 🚀 Запуск проекта

### Шаг 1: Установка зависимостей

```bash
npm install
```

Или если используете yarn:
```bash
yarn install
```

Или pnpm:
```bash
pnpm install
```

### Шаг 2: Запуск dev-сервера

```bash
npm run dev
```

Проект будет доступен по адресу: http://localhost:3000

### Шаг 3: Сборка для production

```bash
npm run build
npm run start
```

## 📝 Добавление контента

### Создание статьи

1. Создайте файл `.mdx` в папке `content/blog/`
2. Добавьте фронтматтер:

```mdx
---
title: "Заголовок вашей статьи"
description: "Краткое описание"
date: "2025-01-09"
tags: ["iOS", "Атрибуция"]
status: "published"
---

Текст статьи в формате Markdown...
```

### Создание инструмента

1. Создайте файл `.mdx` в папке `content/tools/`
2. Добавьте фронтматтер:

```mdx
---
title: "Название инструмента"
description: "Описание"
date: "2025-01-09"
type: "MMP"
status: "active"
tags: ["MMP"]
links:
  primary: "https://example.com"
  docs: "https://docs.example.com"
---

Подробное описание инструмента...
```

## 🎨 Кастомизация

### Изменение цветов

Отредактируйте файл `tailwind.config.ts`:

```typescript
colors: {
  'deep-black': '#0a0a0a',
  'dark-gray': '#151515',
  'accent-cyan': '#00d9ff',
  'accent-cyan-dark': '#00a3cc',
}
```

### Добавление логотипа

Разместите файл `logo.png` в папке `public/brand/`

Затем обновите компонент Header (`components/Header.tsx`):

```typescript
<Image src="/brand/logo.png" alt="Logo" width={40} height={40} />
```

## 🌐 Деплой

### Vercel (рекомендуется)

1. Создайте аккаунт на https://vercel.com
2. Подключите ваш Git репозиторий
3. Vercel автоматически определит Next.js и настроит деплой

### Другие платформы

Проект совместим с:
- Netlify
- Railway
- Render
- DigitalOcean App Platform

## 🔧 Возможные проблемы

### Ошибка при установке зависимостей

Попробуйте очистить кеш:
```bash
npm cache clean --force
npm install
```

### Порт 3000 занят

Измените порт:
```bash
npm run dev -- -p 3001
```

### Ошибки TypeScript

Убедитесь, что установлена правильная версия TypeScript:
```bash
npm install typescript@latest --save-dev
```

## 📞 Поддержка

Если возникли вопросы:
- Telegram: https://t.me/gipoteza_zakryta
- GitHub Issues: создайте issue в репозитории

---

**Удачи с запуском! 🚀**
