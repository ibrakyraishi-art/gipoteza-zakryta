# 🚀 Yango Play Bridge — Инструкция по настройке

## 📋 Что это?

Одностраничный сайт для перенаправления пользователей из Google Ads в приложение Yango Play через AppsFlyer с сохранением всех параметров атрибуции.

---

## 🎯 Как это работает?

### Схема потока:

```
Google Ads (UTM) → Твой сайт → AppsFlyer (af_*) → Yango Play App
```

### Пример преобразования параметров:

| Google Ads (UTM)         | AppsFlyer           | Описание              |
| ------------------------ | ------------------- | --------------------- |
| `utm_source=google`      | `pid=google`        | Источник трафика      |
| `utm_campaign=summer`    | `c=summer`          | Название кампании     |
| `utm_term=yango+play`    | `af_keywords=...`   | Ключевые слова        |
| `utm_content=adgroup_1`  | `af_adset=...`      | Группа объявлений     |
| `gclid=abc123`           | `af_ad=abc123`      | Google Click ID       |
| `utm_medium=cpc`         | `af_channel=cpc`    | Канал (CPC, Display)  |

---

## ⚙️ Настройка

### Шаг 1: Получи AppsFlyer OneLink

1. Зайди в [AppsFlyer Dashboard](https://hq1.appsflyer.com/)
2. Выбери приложение **Yango Play**
3. Перейди в **OneLink** → **Create OneLink**
4. Создай новую ссылку для Google Ads
5. Скопируй базовую ссылку (например: `https://yango.onelink.me/AbCd`)

### Шаг 2: Вставь ссылку в код

Открой файл `app/yango-bridge/page.tsx` и замени:

```typescript
// НАЙДИ ЭТУ СТРОКУ:
const oneLinkBase = 'https://yango.onelink.me/XXXXX'

// ЗАМЕНИ НА СВОЮ:
const oneLinkBase = 'https://yango.onelink.me/AbCd' // Твоя реальная ссылка
```

### Шаг 3: Настрой Google Ads

В Google Ads используй **Value Track Parameters** для динамических UTM:

#### Final URL (конечная ссылка):

```
https://твойсайт.com/yango-bridge?utm_source=google_ads&utm_medium=cpc&utm_campaign={campaignid}&utm_term={keyword}&utm_content={adgroupid}&gclid={gclid}
```

#### Параметры для шаблона отслеживания:

- `{campaignid}` — ID кампании
- `{adgroupid}` — ID группы объявлений
- `{keyword}` — Ключевое слово
- `{gclid}` — Google Click ID
- `{creative}` — ID креатива
- `{placement}` — Плейсмент (для Display)

#### Полный пример URL:

```
https://твойсайт.com/yango-bridge
  ?utm_source=google_ads
  &utm_medium=cpc
  &utm_campaign={campaignid}
  &utm_term={keyword}
  &utm_content={adgroupid}
  &gclid={gclid}
  &creative={creative}
```

### Шаг 4: Настрой AppsFlyer Dashboard

1. В AppsFlyer перейди в **Configuration** → **Data Sources**
2. Добавь **Google Ads** как источник данных
3. Подключи свой аккаунт Google Ads
4. Включи **Cost & ROI tracking**
5. Настрой **Conversion tracking** (установки, покупки и т.д.)

---

## 📊 Проверка работы

### Тестирование вручную:

1. Открой в браузере:
   ```
   http://localhost:3000/yango-bridge?utm_source=google_ads&utm_campaign=test&utm_term=yango+play&utm_content=adgroup1&gclid=test123
   ```

2. На странице внизу увидишь **сгенерированную ссылку**:
   ```
   https://yango.onelink.me/AbCd?pid=google_ads&c=test&af_keywords=yango+play&af_adset=adgroup1&af_ad=test123&af_channel=cpc&deep_link_value=home&af_force_deeplink=true
   ```

3. Нажми кнопку **"Открыть Yango Play"**
4. Если всё настроено правильно — откроется приложение Yango Play

### Проверка в AppsFlyer:

1. Зайди в **Dashboard** → **Overview** → **Installs**
2. Фильтруй по **Media Source** = `google_ads`
3. Смотри детализацию по кампаниям, группам, ключевым словам

---

## 🎨 Кастомизация дизайна

### Изменить цвета:

В файле `app/yango-bridge/page.tsx`:

```typescript
// Градиент фона:
className="bg-gradient-to-br from-purple-900 via-blue-900 to-indigo-900"

// Цвет кнопки:
className="bg-gradient-to-r from-purple-600 to-blue-600"
```

### Изменить тексты:

```typescript
// Заголовок:
<h1>Watch. Listen. Play. All in One</h1>

// Описание:
<p>Огромная библиотека контента ждёт тебя...</p>

// Кнопка:
<button>🚀 Открыть Yango Play</button>
```

### Добавить логотип:

1. Положи файл `logo.png` в `public/brand/yango-logo.png`
2. Добавь в код:
   ```jsx
   <Image src="/brand/yango-logo.png" alt="Yango Play" width={200} height={60} />
   ```

---

## 🚀 Деплой на Vercel

### Шаг 1: Добавь страницу в Git

```bash
git add app/yango-bridge/page.tsx YANGO-BRIDGE-SETUP.md
git commit -m "Add Yango Play bridge page"
git push origin main
```

### Шаг 2: Vercel автоматически задеплоит

Твоя страница будет доступна по адресу:
```
https://твойсайт.vercel.app/yango-bridge
```

### Шаг 3: (Опционально) Подключи кастомный домен

1. В Vercel перейди в **Settings** → **Domains**
2. Добавь домен типа `yango.твойдомен.com`
3. Настрой DNS записи как указано
4. Теперь страница будет по адресу: `https://yango.твойдомен.com/yango-bridge`

---

## 📈 Продвинутые настройки

### 1. Передача дополнительных параметров

Можешь добавить любые кастомные параметры:

```typescript
// В app/yango-bridge/page.tsx:
params.append('af_sub1', searchParams.get('custom_param_1') || '')
params.append('af_sub2', searchParams.get('custom_param_2') || '')
```

### 2. A/B тестирование

Передай вариант креатива:

```typescript
const creative = searchParams.get('creative') || ''
if (creative) params.append('af_ad_type', creative)
```

### 3. Deep Linking (открытие конкретной страницы)

Например, сразу открыть страницу подписки:

```typescript
params.append('deep_link_value', 'subscription') // Вместо 'home'
```

### 4. Geo-таргетинг

Добавь параметр страны:

```typescript
const country = searchParams.get('country') || ''
if (country) params.append('af_geo', country)
```

---

## 🔍 Отладка

### Проблема: Кнопка не работает

**Решение:** Открой консоль браузера (F12) и проверь:
- Правильно ли сгенерирована ссылка?
- Есть ли ошибки JavaScript?

### Проблема: AppsFlyer не показывает данные

**Решение:**
1. Проверь, что OneLink правильно настроен
2. Убедись, что Google Ads подключён как источник
3. Подожди 5-10 минут — данные приходят с задержкой

### Проблема: Приложение не открывается

**Решение:**
1. Проверь, установлено ли приложение Yango Play
2. Попробуй на реальном телефоне (не в эмуляторе)
3. Проверь, что `af_force_deeplink=true` установлен

---

## 📞 Поддержка

Если что-то не работает:

1. **Проверь логи в консоли браузера** (F12)
2. **Проверь сгенерированную ссылку** (она отображается внизу страницы)
3. **Открой ссылку вручную** — должно перенаправить в приложение
4. **Обратись в поддержку AppsFlyer** — они очень отзывчивые

---

## ✅ Чеклист перед запуском

- [ ] Получил OneLink ссылку из AppsFlyer
- [ ] Вставил ссылку в код (`oneLinkBase`)
- [ ] Настроил Final URL в Google Ads с UTM параметрами
- [ ] Подключил Google Ads в AppsFlyer Dashboard
- [ ] Протестировал вручную с тестовыми параметрами
- [ ] Задеплоил на Vercel
- [ ] Запустил тестовую кампанию в Google Ads
- [ ] Проверил данные в AppsFlyer через 10-15 минут

---

## 🎉 Готово!

Теперь у тебя полноценный **Web-to-App Bridge** с отслеживанием всех параметров из Google Ads в AppsFlyer!

**Успехов с кампаниями! 🚀**
